import { StyleSheet } from 'react-native';
import { fontFamilies } from '@theme';

export const styles = StyleSheet.create({
  // The currency code always speaks the brand display face.
  code: {
    fontFamily: fontFamilies.display,
  },
});
