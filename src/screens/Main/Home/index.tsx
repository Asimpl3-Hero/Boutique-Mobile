import React from 'react';
import { ScreenPlaceholder } from '@components/ux';

/** Placeholder — the product list lands in mobile-03. */
export const HomeScreen = () => (
  <ScreenPlaceholder title="Home" subtitle="Products coming soon" />
);
